export const db_path = './db/db.json'

export const config = {
    api: {
        folders: {
            upload: './uploaded'
        },
        res: {
            serverError: 'Internal server error',
            badReq: 'Bad request',
        }
    }
}